# Personal Academic Profile — Chi-An Chen

> 2026-09-14 全站整合完成：Home／Research／Experience／About 已統一 editorial 系統，四個舊路徑保留相容頁。最新架構、建置與瀏覽器驗收以 [deployment-readiness.md](deployment-readiness.md) 為準；下方 Phase 1 與舊七頁紀錄保留為歷史。

> 2026-09-14 最新狀態：Home／Research editorial 原型已完成，採 Source Serif 4／Source Sans 3、暖白與赭色、圖文交替的開放式構圖。敘事順序為農業 VLM → 合作推理研究 → CV／工程延伸，不包含語音研究；兩頁正文不縮放、不使用頁面過場。設計依據見 [redesign-plan.md](redesign-plan.md)，最新畫面與驗收見 [phase1-review.md](phase1-review.md)。以下保留先前版本的歷史紀錄，其動態規格不適用於新的兩頁原型。

2026-09-12 · 七頁 Astro 原型

## 本輪五頁延伸

七頁與交叉連結均已完成，沿用 Home／Education 的色彩、字體、間距和 136／88 px 圓形頭像。本輪只補首頁跨頁入口，不重新設計首頁。Experience 使用原本的日期／內容雙欄；Skills 使用能力段落與工具說明；Research 分研究方向與書目；Honors 分論文獎與競賽；Certifications 以名稱、機構、日期的開放列表分資格與課程，手機轉為單欄。

1100 px 以下導覽改用原生 details／summary。選單在正常文件流展開，使用兩欄文字入口，無需 JavaScript；不使用遮罩、浮動卡片或隱藏於橫向捲動中的選項。桌面與手機都用 aria-current 標示當前頁。完整導覽共用同一份頁面資料。

以下保留首輪兩頁原型的設計與驗收紀錄作為歷史；其中「本輪只完成兩頁」和未完成頁面的敘述只適用於首輪。七頁完成狀態與新一輪驗收以本節及文末為準。

## 設計判斷與本輪修正

首頁先回答「這個人是誰、在哪裡求學、有什麼能力」，再由分類帶讀者深入。姓名、教育身分與自然簡介是主要閱讀重點。保留暖白、墨綠、Georgia 姓名標題與系統無襯線正文；不重做三案風格探索。

本輪移除舊方向稿左側的大照片獨立欄。姓名介紹區使用完整內容寬度，右上方只留一枚小型圓形頭像；簡介與入口緊接姓名身分，下方以兩個開放區段呈現學歷與能力。沒有因照片縮小而留下整欄空白，也沒有 repo 清單、技能徽章或自評百分比。

主要入口為 **Explore my education**，前往已完成的 Education。導覽只顯示 **Home、Education**。其餘五頁仍保留內容規劃，完成後再加入連結；不顯示假按鈕、空連結或可點的占位頁。GitHub 是頁尾可選社群入口。

## 首頁層級與向下延伸

1. **共用導覽：** 姓名連回 Home；Home、Education 文字入口標示目前頁面。手機仍直接顯示兩個入口，不增加選單開關或操作步驟。
2. **人物介紹：** Chi-An Chen 是最大文字；M.S. Student in Computer Science and Information Engineering、National Ilan University, Taiwan 緊接其後。小圓形頭像在介紹區右上方。
3. **簡介與行動：** 保留已確認的兩句背景介紹；Explore my education 是唯一主要行動。
4. **學歷與能力：** 桌面兩欄、手機先學歷再能力。碩士與學士各有系所、學校、日期；能力分 Computer vision、AI applications、Data & deployment，附簡短任務說明。
5. **經歷、研究與學習：** 延伸 PAPAGO 實習與 NIU 背景、研究／獲獎、持續學習摘要。保留自然文案，暫不連到未完成內頁。
6. **頁尾：** 姓名、短定位、已完成站內頁及 LinkedIn／GitHub；不提供原始履歷或證書附件。

較矮的筆電上，學歷與能力摘要自然延伸至下方；不縮小正文以硬塞所有內容。正式英文文案及七頁用途見 [content-outline.md](content-outline.md)。

## 照片規格

使用同一張使用者提供的正式肖像，原件留在本機且不修改。網站資產是 480 × 663 px 的等比例 WebP 衍生檔，已移除中繼資料，不改色、不修臉、不生成背景或替代人物。

- 桌面直徑 **136 px**，位於姓名介紹區右上方；原要求範圍 120–144 px。
- 手機直徑 **88 px**，位於姓名右側；原要求範圍 80–96 px。
- CSS 使用 `border-radius: 50%`、`object-fit: cover`、`object-position: 50% 0%`。圓形框裁去原照下半部衣著，髮頂、臉部與下巴須完整保留，比例不變形。
- 圖片具有明確寬高與英文替代文字，預先保留位置以減少版面跳動。衍生檔保留矩形內容，圓形裁切只在 CSS 顯示層發生。

## 七頁架構與內頁樣板

| 頁面／獨立路徑 | 內容與版面 | 本輪狀態 |
| --- | --- | --- |
| Home `/` | 姓名、小頭像、教育身分、簡介、學歷及能力摘要、背景延伸 | 已實作 |
| Education `/education/` | 碩士、學士依時間倒序；日期左欄、學位／學校／背景右欄；補充課外學習 | 已實作 |
| Experience `/experience/` | 實習、研究助理與產學角色，短任務敘述 | 待實作 |
| Skills `/skills/` | 按任務分組的能力與工具 | 待實作 |
| Research `/research/` | 研究問題、題目、發表資訊與摘要 | 待實作 |
| Honors `/honors/` | Paper awards／Competition awards，區分事件與團隊歸屬 | 待實作 |
| Certifications `/certifications/` | Credentials／Courses & workshops，名稱、機構與時間 | 待實作 |

七頁仍是獨立路徑，並未改為首頁錨點。Education 作為內頁樣板：一個清楚的頁名、一段短導言、開放式內容列表與回 Home 入口。手機把日期放到學位上方。Home 與 Education 共用學歷資料，避免日期或身分不一致。

Experience 回答角色與任務，Research 回答研究問題與成果，Honors 回答認可事件。後續透過跨頁連結建立關係，不複製整段文字；公開 repo 仍不是收錄條件。

## 設計系統與操作

| 項目 | 原型規格 |
| --- | --- |
| 內容寬度 | 最大 1120 px；小螢幕依斷點使用 48／32／20 px 邊距 |
| 背景／主文字 | `#F6F4EE`／`#243B35` |
| 正文／次要文字 | `#3D4842`／`#626B64` |
| 連結／分隔線 | `#355C4B`／`#CFD5CC` |
| 姓名／頁名 | Georgia，桌面最大 96 px，依視窗縮放；手機最低 44 px |
| 介紹／正文 | 系統無襯線，20／17 px；手機介紹 18 px；行高 1.65–1.7 |
| 主要入口 | 墨綠矩形、3 px 圓角、至少 48 px 高 |
| 導覽與焦點 | 語意化 nav、目前頁面標示、可見焦點外框、Skip to content |
| 動態 | 預設靜態，無必要動畫；尊重 prefers-reduced-motion |

文字配色相對背景的計算對比：主文字 10.90:1、正文 8.66:1、次要文字 5.02:1、連結 6.86:1。採系統字體，無外部字體請求。兩頁內容、導覽與照片均不依賴客戶端 JavaScript。

## 實際瀏覽器驗收

本輪以本機 Chrome／Playwright 檢查 production build，以下均已完成；原始檢查輸出留在 work/，不混入網站文案。

| 檢查 | 結果 |
| --- | --- |
| Astro 檢查與建置 | 0 errors、0 warnings；輸出 Home 與 Education 兩個靜態頁面 |
| 桌面 1536 × 1024 | 姓名、身分、介紹與入口完整可讀；頭像 136 × 136 px；下方學歷與能力形成清楚的第二閱讀層 |
| 小型筆電 1280 × 800 | 首頁姓名／介紹／入口不裁切；學歷與能力標題在下緣出現，內容自然往下延伸 |
| 手機 390 × 844 | 頭像 88 × 88 px 並列於姓名右側；教育身分跨完整寬度；Education 日期改到學位上方 |
| 窄螢幕 320 × 740 | 兩頁無橫向溢出，姓名自然換行；照片不拉伸 |
| 鍵盤 | 桌面與手機尺寸逐項 Tab、Shift+Tab，焦點可見；Skip to content 能移至 main；Enter 能由首頁入口到 Education |
| 真實連結 | 兩頁站內連結回應 200；五個未完成路徑不生成頁面，回應 404；導覽不露出它們 |
| 無 JavaScript／減少動態 | 兩頁仍可閱讀、顯示照片並完成跨頁；正式輸出無客戶端 script |
| 文字放大 | 1280 px 視窗以 200% 根字級檢查，兩頁無橫向溢出 |
| 公開輸出 | 只含兩份 HTML、共用 CSS、頭像與 favicon；不含來源 PDF、本機路徑或查核語句 |
| 本機資料獨立性 | 只複製 src、public 與設定的隔離目錄可建置；以 `/personal-website/` base 建置，站內與資產路徑靜態解析皆有效 |

逐張目視桌面、小型筆電與手機畫面，圓框內髮頂、臉部、下巴完整且沒有變形。首頁及 Education 全頁內容均可捲動閱讀，頁尾沒有壓住文字。此處為 Chrome 的響應式及鍵盤檢查，尚未涵蓋實體手機或螢幕閱讀器完整驗收。

實際瀏覽器截圖（均位於 Git 忽略目錄）：

- `docs/design-exploration/prototype-home-desktop.png`、`prototype-home-laptop.png`、`prototype-home-mobile.png`
- `docs/design-exploration/prototype-education-desktop.png`、`prototype-education-laptop.png`、`prototype-education-mobile.png`
- 同名 `-full.png` 版本保留完整頁面，方便檢查下方內容與頁尾。

以「是否認識我、能否看見學歷與能力、分類是否清楚」檢查：姓名及真實頭像先建立人物辨識，碩士身分立即可見，兩個學位與三組能力往下展開；已完成頁面的分類和路徑清楚，七頁完整規劃仍保留。

## 歷史保留與邊界

舊 `home-personal-academic-profile.png` 是大照片方向稿，保留但其照片規格已被本輪取代。舊 A／B／C 圖片及提示詞／manifest 原樣保留；其 repo 主導的首頁定位與推薦仍已淘汰。所有方向稿與實際瀏覽器截圖位於 Git 忽略的 docs/design-exploration/。

本輪只完成 Home、Education 與共用框架、必要依賴及 lockfile。來源、原始照片、PDF、私密資訊與查核筆記留在本機；僅選定文案及必要圖片衍生檔進入網站。保留第三方 skills 與既有變更，不提交、不上傳、不部署。

## 七頁完成後驗收（2026-09-12）

- `npm run check`：0 errors、0 warnings；`npm run build`：七頁靜態輸出。既有套件與 lockfile 不變，沒有新增或升級依賴。
- 沿用 127.0.0.1:4322 本機 preview，沒有新增開發或預覽服務。
- Chrome：1536 × 1024 桌面、1280 × 800 小型筆電、390 × 844 手機、320 × 740 窄螢幕；七頁直接載入與重新整理皆成功，無橫向溢出，每頁有獨立標題與描述。
- 七頁站內路徑皆回應 200；桌面選單逐個用 Enter 開啟並重新整理。Home、Education 已補齊對應跨頁入口。
- 1280／390 px 七頁逐項 Tab、Shift+Tab；Skip to content 移至 main，焦點可見，當前頁標示正確。手機選單可用 Enter 展開、Space 關閉；關閉 JavaScript 並設定 reduced-motion 後，仍能從選單走遍七頁並閱讀內容。
- 公開輸出只含七份 HTML、共用 CSS、頭像與 favicon，沒有客戶端 script。沒有空入口、原始 PDF、證號、成績單、原始照片檔或本機來源路徑。
- 在只含 src、public、設定與依賴的隔離目錄完成建置，未複製 reference_data、work 或內容 Markdown。以 `/personal-website/` base 建置後，214 個本機連結與資產參照靜態解析有效。
- 原始資料、第三方 skills、歷史畫面、頭像衍生檔與套件檔均保持本輪起始雜湊；Git 索引不含 reference_data、work、PDF 或探索稿。

新畫面為 docs/design-exploration/seven-{頁名}-{desktop|laptop|mobile}.png，同名 -full.png 保留全頁；seven-mobile-menu.png 展示無 JavaScript 手機選單，seven-research-publications.png 展示論文書目。截圖維持 Git 忽略，舊稿原樣保留。

本輪完成七頁 Chrome 響應式、鍵盤與無 JavaScript 檢查，未做實體手機、Safari 或完整螢幕閱讀器驗收。來源對照與少量待確認事項在 work/seven-pages/editorial-notes.md。不提交、不上傳、不部署。

## 第一輪輕微動態（2026-09-13）

本輪在原有七頁設計上加入操作回饋與一處閱讀節奏，不更動姓名、正文、照片、間距或頁面內容。採純 CSS；沒有新增依賴或客戶端腳本。

| 位置 | 實際效果 | 選擇理由 |
| --- | --- | --- |
| 七頁主要按鈕／文字入口 | 箭頭向右上移動 2 px／160 ms；實心按鈕按壓縮至 98% | 以現有箭頭回應操作，保留原本點擊範圍和導頁行為 |
| 桌面七頁導覽 | 單一底線由左至右展開／180 ms；目前頁面底線常駐 | 表達可操作性與目前位置，不加入圖示或浮動背景 |
| Home 下方 Across research and industry | 隨捲動由 88% → 100%，左上基準；反向捲動縮回（依回饋加強原先 96% 起點） | 在教育與能力摘要之後建立輕微節奏，人物身分與長篇內容維持穩定 |

標題採 `view()` 與 `animation-range: entry 0% cover 35%`；只改變 transform，不隱藏文字。讓該標題在 Grid 中靠上對齊，使捲動時間軸依標題本身高度計算，而非依整個內容列的高度計算。桌面最大縮放不超過原尺寸。

依後續回饋，桌面與手機均啟用標題縮放；不支援所需 CSS 的瀏覽器保持原尺寸。原生手機選單不新增動畫。減少動態時取消縮放、位移與時間過渡，顏色、底線及鍵盤焦點仍即時可見。正文、姓名與照片沒有入場延遲；不添加換頁動畫。以下首輪驗收中的 96% 起點與手機停用紀錄為調整前結果，最新驗收見文末。

### 正式建置驗證

- Astro check：0 errors、0 warnings；七頁建置及既有內容檢查通過，221 個本機參照、10 個公開產物，沒有 script。
- Chrome 153：1536 × 1024、1280 × 800、390 × 844，七頁直接開啟與重整共 21 組均成功，49 個站內交叉連結回應 200，沒有橫向溢出。
- 實測每頁文字入口箭頭位移為 `(2, -2)` px、過渡 160 ms；按鈕按壓縮放為 0.98。桌面底線正常展開，當前頁常駐，無重複底線；鍵盤 focus-visible 與 Enter 導頁正常。
- 1536／1280 px 的標題起點為 0.96、中段介於 0.96–1、終點為 1，反向捲動回到 0.96。測量期間排版位置、字級與文件高度保持一致，文字不透明度始終為 1。
- 關閉 JavaScript 的桌面仍可執行 CSS 捲動縮放；390 px 觸控模擬停用縮放，原生手機選單可用鍵盤走遍七頁。減少動態下按鈕、箭頭與標題沒有 transform 動畫，導覽底線仍可辨識。
- 透過僅作用於測試瀏覽器的 CSS 回應替換，令 `@supports` 條件不成立：標題為原尺寸、完全不透明、無動畫。此項是模擬退路，未宣稱已在不支援功能的實際瀏覽器通過測試。
- 正式建置最初將 animation 和 view() 合併成不被目前 Chrome 接受的簡寫；改用個別 animation 屬性後，重新建置並通過上述動畫量測。套件與建置設定保持原樣。

本輪使用既有 4322 預覽服務。截圖與短示範置於 Git 忽略的 `docs/design-exploration/motion-*`，測試紀錄留在 work/。尚未在 Safari、Firefox、實體觸控手機或完整螢幕閱讀器流程實測。不提交、不推送、不部署。

示範影片：`docs/design-exploration/motion-preview.mp4`，1536 × 1024、10 fps、13.7 秒，由實際瀏覽器連續畫面經 macOS 內建編碼器產生；已解碼抽查 0／4／8／12 秒畫面。依序展示桌面導覽、按鈕／箭頭和標題正反向捲動。手機仍需以實體裝置確認觸控點放的瞬間回饋。

觸控補測：Chrome 觸控模擬在 tap 點放後進入短暫 `:active`，量測縮放由 1 經 0.984744、0.980017 再回到 1；箭頭維持無位移。測試期間僅在測試瀏覽器攔截該次點擊導頁，以便量測瞬間狀態，網站本身沒有加入事件腳本。觸控按住期間不保證持續顯示縮放，依瀏覽器原生 `:active` 時序呈現。

### 加強幅度與手機啟用（2026-09-13，後續調整）

依使用者回饋，縮放起點從 96% 改為 88%，終點保持 100%，並移除手機寬度限制。原有時間軸、左上基準、透明度與其他互動不變。所有尺寸均以 CSS 功能支援與使用者動態偏好決定是否啟用，無支援或要求減少動態時顯示原尺寸靜態文字。

正式建置後，1536／1280／390 px 在關閉 JavaScript 的瀏覽器情境中，均量測到 0.88 → 約 0.94 → 1，反向回到 0.88；字級、文件高度與排版位置不變，無橫向溢出。三種尺寸的 reduced-motion 與模擬 @supports 不成立測試皆回到無 transform 的靜態。手機為 Chrome 觸控模擬，尚未在實體手機或 Safari／Firefox 實測。

最新版短示範為 `motion-stronger-desktop.mp4` 與 `motion-stronger-mobile.mp4`（各 5.4 秒），位於 Git 忽略的 docs/design-exploration/。原 `motion-preview.mp4` 保留為前一版的歷史示範。

### 七頁主內容全文縮放（2026-09-13，最新規格）

依使用者要求，從單一首頁標題擴展為七頁 main 內所有可見文字：h1–h3、段落、dt／dd、工作任務列表和文字入口。頁首與頁尾導覽、頭像不納入。此節取代前述只套用單一標題的範圍規格。

保留使用者手動設定的 60% → 100% 與 `entry 0% cover 55%`，以每個文字區塊自身的 view() 時間軸計算，反向捲動縮回。段落以整段為單位，內部日期、強調字及行內文字一併縮放，不另套父層紀錄／section 動畫，避免重複縮放。此幅度比前版明顯，畫面下緣的文字會較小，隨閱讀捲動逐步恢復原尺寸。

主要按鈕與文字入口新增 `.motion-label` 內層，只有內容隨捲動縮放；外層保留原來的 44–48 px 點擊高度、焦點與按壓回饋，箭頭移動依然正常。無新增依賴、客戶端腳本、換頁動畫或照片效果。CSS 功能不足或 reduced-motion 時整個主內容回到靜態原尺寸。

正式建置與既有內容檢查通過，七頁 221 個本機參照有效。Chrome 在 1536 × 1024、1280 × 800、390 × 844 共 21 組檢查中，所有主內容文字節點均恰由一個動畫區塊涵蓋；無漏套、重複套用或橫向溢出，頁首／頁尾未納入動畫，頭像沒有 transform。每頁均檢查鍵盤焦點、箭頭回饋及 reduced-motion。另以無 JavaScript 的手機觸控模擬量測 60% → 約 80% → 100% → 60%，原生選單可導頁，模擬不支援 CSS 時回到靜態。未實測實體手機、Safari、Firefox。

最新靜態畫面為 docs/design-exploration/all-text-{home|experience}-{1536|1280|390}.png，舊單一標題示範影片保留作歷史，不代表本輪全文效果。驗收資料位於 work/motion-all-text/，均維持 Git 忽略。本輪尚未提交或推送。
